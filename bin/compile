#!/usr/bin/env bash

# This script is deprecated as a buildpack stage.
# However, we keep it to preserve backwards compatability with
# Heroku and older versions of CloudFoundry
# See: https://docs.cloudfoundry.org/buildpacks/understand-buildpacks.html#buildpack-scripts

exit
